const fruits = ['apple', 'banana', 'orange'];

for (let fruit of fruits) {
  console.log(fruit);
}
const numbers = [1, 2, 3, 4, 5];

for (let number of numbers) { // iterating over the numbers array
  console.log(number + 1); // For each number, add 1 and log it
}
